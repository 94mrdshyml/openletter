{
	"name": "openletter",
	"version": "0.1.0",
	"description": "CLI to scaffold, provision, and deploy a self-hosted OpenLetter publication on Cloudflare",
	"type": "module",
	"bin": {
		"openletter": "./bin/openletter.ts"
	},
	"files": [
		"bin",
		"src",
		"README.md"
	],
	"license": "MIT",
	"repository": {
		"type": "git",
		"url": "https://github.com/94mrdshyml/openletter.git",
		"directory": "cli"
	},
	"engines": {
		"bun": ">=1.0.0"
	},
	"scripts": {
		"lint": "prettier --check . && eslint .",
		"format": "prettier --write .",
		"check": "tsc --noEmit"
	},
	"dependencies": {
		"@clack/prompts": "^0.11.0",
		"commander": "^14.0.0"
	},
	"devDependencies": {
		"@types/node": "^24",
		"typescript": "^6.0.3"
	}
}
